# M5 資格檢核器 - 批次檢核功能完成報告

## 概述

根據使用者需求：「資格審核只要選取名字就可以查 不需要身分證字號。另外要支援多選查詢 除了單獨匯出結果也要支援多選後批次匯出的功能」，已完成資格檢核器的批次檢核和匯出功能。

## 修改日期

2026-01-04

## 改動摘要

### ✅ 新增功能

1. **移除身分證字號驗證**
   - 只用姓名查詢即可執行檢核
   - 簡化檢核流程
   - 提升操作便利性

2. **雙模式檢核**
   - 單一檢核：選擇單一員工進行檢核
   - 批次檢核：多選員工進行批次檢核

3. **批次檢核功能**
   - 支援 multiselect 多選員工
   - 顯示進度條和檢核進度
   - 自動儲存所有檢核結果

4. **檢核結果管理**
   - 批次檢核結果摘要統計
   - 結果表格展示
   - 清除檢核結果功能

5. **批次匯出功能**
   - 單一匯出：匯出個別員工檢核報告
   - 批次匯出：一次匯出多位員工的檢核報告
   - Excel 多分頁格式（檢核摘要 + 檢核明細）

## 修改的檔案

### `modules/m5_qualification_check.py`

#### 1. 更新 check() 方法（第 70-106 行）

**修改內容：**

```python
def check(self, name: str, id_number: str = None) -> Dict[str, Any]:
    """
    執行資格檢核

    Args:
        name: 姓名
        id_number: 身分證字號（選填，若不提供則只用姓名查詢）

    Returns:
        檢核結果字典
    """
    result = {
        'name': name,
        'id_number': id_number if id_number else 'N/A',
        'checks': [],
        'overall_status': 'PENDING',
        'timestamp': datetime.now().strftime('%Y-%m-%d %H:%M:%S')
    }

    # Step 1: 查詢員工基本資料
    employee = self._find_employee_by_name(name) if not id_number else self._find_employee(id_number)
```

**變更：**
- `id_number` 參數改為選填（預設 None）
- 當沒有 id_number 時，使用姓名查詢
- 支援兩種查詢方式：姓名查詢 / 身分證字號查詢

#### 2. 新增 _find_employee_by_name() 方法（第 171-181 行）

```python
def _find_employee_by_name(self, name: str) -> Optional[Dict]:
    """根據姓名查詢員工"""
    conn = self.db_employees._get_connection()
    cursor = conn.cursor()
    cursor.execute("SELECT * FROM employees WHERE name = ?", (name,))
    row = cursor.fetchone()
    conn.close()

    if row:
        return dict(row)
    return None
```

**功能：**
- 直接使用姓名查詢員工資料
- 不需要身分證字號

#### 3. 重構 Tab 1 檢核界面（第 286-387 行）

**新增檢核模式選擇：**

```python
# 選擇檢核模式
check_mode = st.radio(
    "檢核模式",
    options=["單一檢核", "批次檢核"],
    horizontal=True
)
```

**單一檢核模式：**
```python
if check_mode == "單一檢核":
    # 單一員工檢核
    selected_option = st.selectbox("選擇要檢核的員工", ...)

    if st.button("🔍 執行檢核", type="primary"):
        # 使用姓名直接檢核（不需要身分證字號）
        result = st.session_state.checker.check(selected_emp['name'], None)
        st.session_state.last_check_result = result
        st.session_state.check_results[selected_emp['emp_id']] = result
```

**批次檢核模式：**
```python
else:
    # 批次檢核
    selected_options = st.multiselect(
        "選擇要檢核的員工（可多選）",
        options=employee_options,
        help="可以使用 Ctrl/Cmd 多選，或在下拉選單中逐一勾選"
    )

    if st.button("🔍 批次執行檢核", type="primary"):
        progress_bar = st.progress(0)
        status_text = st.empty()

        for idx, option in enumerate(selected_options):
            emp_name = option.split(" - ")[1].split(" (")[0]

            # 執行檢核
            result = st.session_state.checker.check(emp_name, None)
            st.session_state.check_results[emp_id] = result

            progress_bar.progress((idx + 1) / len(selected_options))
```

#### 4. 批次檢核結果摘要（第 389-429 行）

```python
if st.session_state.check_results:
    st.divider()
    st.subheader("📊 批次檢核結果摘要")

    # 統計各狀態數量
    status_counts = {'APPROVED': 0, 'REVIEW_REQUIRED': 0, 'REJECTED': 0, 'NOT_FOUND': 0}
    for result in st.session_state.check_results.values():
        status = result.get('overall_status', 'NOT_FOUND')
        status_counts[status] = status_counts.get(status, 0) + 1

    # 顯示統計卡片
    col1, col2, col3, col4 = st.columns(4)
    with col1:
        st.metric("✅ 建議核准", status_counts['APPROVED'])
    with col2:
        st.metric("⚠️ 需要審查", status_counts['REVIEW_REQUIRED'])
    with col3:
        st.metric("❌ 不建議核准", status_counts['REJECTED'])
    with col4:
        st.metric("📊 總計", len(st.session_state.check_results))

    # 顯示結果表格
    with st.expander("📋 查看詳細結果", expanded=True):
        results_df = pd.DataFrame(results_list)
        st.dataframe(results_df, use_container_width=True)
```

#### 5. 批次匯出功能（第 431-490 行）

```python
if 'show_batch_export' in st.session_state and st.session_state.show_batch_export:
    st.divider()
    st.subheader("📥 批次匯出檢核報告")

    # 建立批次報告
    all_summaries = []
    all_details = []

    for emp_id, result in st.session_state.check_results.items():
        # 摘要資料
        all_summaries.append({
            '工號': emp_id,
            '姓名': result.get('name', 'N/A'),
            '檢核時間': result.get('timestamp', 'N/A'),
            '檢核狀態': status_text_map.get(result.get('overall_status'), '未知'),
            '系統建議': result.get('recommendation', 'N/A'),
            '審查要點': result.get('review_notes', 'N/A')
        })

        # 明細資料
        for check in result.get('checks', []):
            all_details.append({
                '工號': emp_id,
                '姓名': result.get('name', 'N/A'),
                '檢核項目': check.get('item', 'N/A'),
                '狀態': check.get('status', 'N/A'),
                '詳細說明': check.get('detail', 'N/A')
            })

    # 生成 Excel 檔案
    summary_df = pd.DataFrame(all_summaries)
    details_df = pd.DataFrame(all_details)

    output = BytesIO()
    with pd.ExcelWriter(output, engine='openpyxl') as writer:
        summary_df.to_excel(writer, index=False, sheet_name='檢核摘要')
        details_df.to_excel(writer, index=False, sheet_name='檢核明細')

    st.download_button(
        label=f'💾 下載批次檢核報告 ({len(st.session_state.check_results)} 位員工)',
        data=output,
        file_name=f"batch_qualification_check_{datetime.now().strftime('%Y%m%d_%H%M%S')}.xlsx",
        mime='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
    )
```

## 功能對比

### 之前

❌ **限制：**
- 需要輸入身分證字號才能檢核
- 只能單一員工檢核
- 只能單一匯出報告
- 無法批次處理

### 現在

✅ **改善：**
- 只需選擇姓名即可檢核（不需身分證字號）
- 支援單一檢核和批次檢核兩種模式
- 支援多選員工（multiselect）
- 顯示批次檢核進度
- 批次檢核結果摘要統計
- 支援批次匯出所有檢核結果
- 單一匯出和批次匯出並存

## 使用流程

### 單一檢核流程

1. **選擇模式**：點選「單一檢核」
2. **選擇員工**：從下拉選單選擇員工
3. **執行檢核**：點擊「🔍 執行檢核」
4. **查看結果**：檢視檢核報告
5. **匯出報告**：點擊「📥 匯出此檢核報告」

### 批次檢核流程

1. **選擇模式**：點選「批次檢核」
2. **多選員工**：
   - 點擊下拉選單
   - 使用 Ctrl/Cmd 多選
   - 或逐一勾選員工
3. **執行檢核**：點擊「🔍 批次執行檢核」
4. **查看進度**：觀察進度條和檢核進度
5. **查看摘要**：
   - 統計卡片（核准/審查/拒絕/總計）
   - 詳細結果表格
6. **批次匯出**：點擊「📥 批次匯出報告」
7. **下載報告**：點擊「💾 下載批次檢核報告 (X 位員工)」

## 界面展示

### 批次檢核摘要

```
📊 批次檢核結果摘要

┌──────────────┬──────────────┬──────────────┬──────────────┐
│ ✅ 建議核准   │ ⚠️ 需要審查   │ ❌ 不建議核准  │ 📊 總計       │
│     5        │     2        │     1        │     8        │
└──────────────┴──────────────┴──────────────┴──────────────┘

📋 查看詳細結果
┌────────┬────────┬──────────────┬────────────────┬─────────────┐
│ 工號   │ 姓名   │ 檢核狀態      │ 系統建議        │ 檢核時間     │
├────────┼────────┼──────────────┼────────────────┼─────────────┤
│ E001   │ 張三   │ ✅ 建議核准   │ 所有項目通過   │ 2026-01-04  │
│ E002   │ 李四   │ ⚠️ 需要審查   │ 有1個警示項目  │ 2026-01-04  │
│ ...    │ ...    │ ...          │ ...            │ ...         │
└────────┴────────┴──────────────┴────────────────┴─────────────┘
```

### 批次匯出報告格式

**Sheet 1: 檢核摘要**
```
工號 | 姓名 | 檢核時間 | 檢核狀態 | 系統建議 | 審查要點
E001 | 張三 | 2026-01-04 10:30:00 | ✅ 建議核准 | 所有檢核項目通過 | N/A
E002 | 李四 | 2026-01-04 10:30:05 | ⚠️ 需要審查 | 有 1 個警示項目 | 建議主管重點評估...
E003 | 王五 | 2026-01-04 10:30:10 | ❌ 不建議核准 | 列於黑名單中 | N/A
```

**Sheet 2: 檢核明細**
```
工號 | 姓名 | 檢核項目 | 狀態 | 詳細說明
E001 | 張三 | 員工資料查詢 | PASS | 工號: E001, 部門: 業務部
E001 | 張三 | 黑名單比對 | PASS | 未列於黑名單
E001 | 張三 | 離職記錄檢查 | PASS | 自願離職，無不良紀錄
E002 | 李四 | 員工資料查詢 | PASS | 工號: E002, 部門: 研發部
E002 | 李四 | 黑名單比對 | PASS | 未列於黑名單
E002 | 李四 | 歷史績效紀錄 | WARNING | 曾有 2 次低績效紀錄
...
```

## 技術細節

### Session State 管理

```python
# 檢核結果儲存
st.session_state.check_results = {
    'E001': {result_dict},
    'E002': {result_dict},
    ...
}

# 單一檢核結果（用於即時顯示）
st.session_state.last_check_result = {result_dict}

# 批次匯出控制
st.session_state.show_batch_export = True/False
```

### 查詢方法選擇邏輯

```python
# 如果有 id_number，使用身分證字號查詢
if id_number:
    employee = self._find_employee(id_number)
# 否則使用姓名查詢
else:
    employee = self._find_employee_by_name(name)
```

### 進度追蹤

```python
progress_bar = st.progress(0)
status_text = st.empty()

for idx, option in enumerate(selected_options):
    status_text.text(f"檢核中... {emp_name} ({idx + 1}/{len(selected_options)})")
    # ... 執行檢核
    progress_bar.progress((idx + 1) / len(selected_options))
```

## 優點

### 1. 操作簡化
- ✅ 不需要記憶或輸入身分證字號
- ✅ 直接從清單選擇員工
- ✅ 降低輸入錯誤風險

### 2. 效率提升
- ✅ 批次檢核節省時間
- ✅ 多選功能方便選擇
- ✅ 進度顯示清楚

### 3. 結果管理
- ✅ 統計摘要一目了然
- ✅ 表格展示便於比較
- ✅ 可以清除重新檢核

### 4. 匯出彈性
- ✅ 單一匯出：針對個別員工
- ✅ 批次匯出：整批匯出方便歸檔
- ✅ Excel 格式易於後續處理

## 測試建議

### 測試案例 1：單一檢核

1. 啟動應用程式
2. 選擇「單一檢核」
3. 選擇員工「E001 - 張志明」
4. 點擊「執行檢核」
5. 查看檢核結果
6. 點擊「匯出此檢核報告」
7. 下載並驗證報告內容

### 測試案例 2：批次檢核（3 位員工）

1. 選擇「批次檢核」
2. 多選 3 位員工（E001, E002, E003）
3. 點擊「批次執行檢核」
4. 觀察進度條和狀態更新
5. 查看摘要統計（應顯示 3 位總計）
6. 展開詳細結果表格
7. 點擊「批次匯出報告」
8. 下載並驗證報告包含 3 位員工資料

### 測試案例 3：批次檢核（全部員工）

1. 選擇「批次檢核」
2. 全選所有員工（假設 8 位）
3. 執行批次檢核
4. 驗證所有員工都被檢核
5. 檢查統計數字正確（核准/審查/拒絕）
6. 批次匯出並驗證 Excel 包含所有員工

### 測試案例 4：清除結果

1. 執行批次檢核後
2. 點擊「清除檢核結果」
3. 驗證結果被清空
4. 可以重新執行檢核

## 總結

### ✅ 完成項目

1. **移除身分證字號驗證** - 只需姓名即可檢核
2. **雙模式檢核** - 單一檢核 + 批次檢核
3. **多選功能** - multiselect 支援
4. **進度追蹤** - 進度條 + 狀態顯示
5. **結果摘要** - 統計卡片 + 表格展示
6. **批次匯出** - 一次匯出多位員工報告
7. **結果管理** - 清除功能

### 💡 優勢

- **操作更簡單**：不需身分證字號
- **效率更高**：批次處理節省時間
- **管理更方便**：摘要統計、表格展示
- **匯出更彈性**：單一/批次兩種方式

### 🎯 使用場景

- **單一檢核**：臨時查詢個別員工資格
- **批次檢核**：定期審查多位離職員工回任資格
- **批次匯出**：製作月報、季報歸檔

**修改完成日期：** 2026-01-04

**修改行數：** 約 200 行（新增批次檢核邏輯和界面）

**測試狀態：** ⏳ 待測試
